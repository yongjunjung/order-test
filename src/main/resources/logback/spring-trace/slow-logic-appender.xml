<?xml version="1.0" encoding="UTF-8"?>
<!-- 애플리케이션 전체 로그 -->
<appender name="SLOW_LOGIC" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_PATH}slow-logic.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
        <fileNamePattern>${LOG_PATH}backup/slow-logic-%d{yyyy-MM-dd}.%i.log.zip</fileNamePattern> <!-- rollover daily -->
        <maxHistory>60</maxHistory>
        <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
            <maxFileSize>100MB</maxFileSize> <!-- or whenever the file size reaches 100MB -->
        </timeBasedFileNamingAndTriggeringPolicy>
    </rollingPolicy>
    <encoder>
        <charset>utf8</charset>
        <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level --- [%thread] %logger{35} : %msg %n</pattern>
    </encoder>
</appender>
